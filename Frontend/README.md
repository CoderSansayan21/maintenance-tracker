# MaintainHub Frontend

## Setup

```bash
npm install
npm run dev
```

Create `.env` if the API is not on localhost:

```env
VITE_API_URL=http://localhost:8000
```

The app runs at `http://localhost:5173`.
